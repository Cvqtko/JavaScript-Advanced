let someArr = [1,2,3,4];

function addElement(arr,el){
	arr.push(el);
}

addElement(someArr, 5);
console.log(someArr);

/* Mutable is a type of variable that can be changed. 
In JavaScript, only objects and arrays are mutable, not primitive values. */